/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "www.doi.gov",
            "storage.googleapis.com"
        ],
    },
};

export default nextConfig;
